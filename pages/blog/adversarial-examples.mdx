import { BlogPost } from "../../components/BlogPost";

export const meta = {
    author: "Tomasz Miśków",
    title: "A comparative study of deep neural networks' immunity to adversarial examples",
};

export default ({children}) => <BlogPost meta={meta}>{children}</BlogPost>;
